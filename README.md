# rotom.gg

Track your Pokémon TCG Live games.

## Commands

- `bin/setup` — from a cold clone to running, database included
- `bin/dev` — start the database and the dev server
- `bin/verify` — format, lint, typecheck, tests, and build

Stack and conventions are decided in [docs/decisions.md](docs/decisions.md).
